name : my first workflow 
on : push 
jobs:
  first-job:
    runs-on : ubuntu-latest
    steps:
    - name : welcome message
      run: echo "hello avinash"
    - name : list file
      run : ls 
    - name : read file 
      run : cat README.md
        
      

